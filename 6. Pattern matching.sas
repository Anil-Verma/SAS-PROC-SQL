* Creating a library for proc_sql_Data;
libname procsql "/home/PATH/Proc_sql/data";


***			lIKE OPERATOR		***;
PROC SQL;
	SELECT PRODNAME,
			PRODCOST,
			PRODTYPE
	FROM PROCSQL.PRODUCTS
	WHERE PRODNAME LIKE 'A%';
QUIT;


PROC SQL;
	SELECT PRODNAME,
			PRODTYPE,
			PRODCOST
	FROM PROCSQL.PRODUCTS
	WHERE UPCASE(PRODTYPE) LIKE 'SOFT%';
QUIT;


***		SPACE AND % ****;
PROC SQL;
	SELECT PRODNAME,
			PRODCOST,
			PRODTYPE
	FROM PROCSQL.PRODUCTS
	WHERE PRODNAME LIKE '% ';
QUIT;


***  _ AND %  **;
PROC SQL;
	SELECT PRODNAME,
			PRODCOST,
			PRODTYPE
	FROM PROCSQL.PRODUCTS
	WHERE PRODTYPE LIKE 'P____';
QUIT;


PROC SQL;
	SELECT PRODNAME,
			PRODTYPE,
			PRODCOST
	FROM PROCSQL.PRODUCTS
	WHERE PRODNAME LIKE '___a%';
QUIT;


